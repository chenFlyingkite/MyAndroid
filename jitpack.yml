# https://www.w3schools.io/file/yaml-rmarkdown-currentdate/

jdk:
  - openjdk11
#before_install:
   #- echo "Running before_install"
   #- ./custom_setup.sh
#install:
   #- echo "Running install"
   #- ./gradlew clean install -xtest